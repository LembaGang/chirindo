// Placeholder product name. The final name has not been chosen.
// This constant is the ONLY place in source where the placeholder appears,
// outside of (1) package.json "name" and (2) the bin entry. See README "Renaming".
//
// It is used only for CLI banner/help text — NEVER in record formats, schemas,
// chain logic, or anything an external consumer would parse.
export const PRODUCT_NAME = "recorder";
